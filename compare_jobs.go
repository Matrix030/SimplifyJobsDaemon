package main

import (
	api "github.com/Matrix030/simplify_jobs_cli/internal/simplifyapi"
)

func compareJobs(oldJobs, newJobs api.Jobs) api.Jobs {
	//this compares the old and the new jobs
	//this also filters out based on sponsorships
	return api.Jobs{}
}
